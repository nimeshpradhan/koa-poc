{
  "serverConfig": {
    "port": "PORT"
  },
  "dbConfig": {
    "postgres": {
      "connectionOptions": {
        "user": "POSTGRES_USER",
        "host": "POSTGRES_HOST",
        "password": "POSTGRES_PWD",
        "port": "POSTGRES_PORT",
        "schema": "POSTGRES_SCHEMA"
      }
    }
  }
}
